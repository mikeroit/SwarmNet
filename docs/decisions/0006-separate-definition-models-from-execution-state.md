# ADR 0006: Separate Definition Models from Execution State

SwarmNet will model static definitions separately from mutable execution state.

Examples:

- Route and RouteExecution
- FlightPlan and FlightPlanExecution
